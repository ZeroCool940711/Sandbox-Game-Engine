//
// Common shader constants for patrol paths, used in the pixel shader.
//

bool colourise = false;
bool highlight = false;

const float colouriseBlend = 0.5f;
const float4 colouriseColour = float4( 0.2f, 0.0f, 0.0f, 1.0f );
const float4 highlightColour = float4( 0.0f, 1.0f, 0.0f, 1.0f );

